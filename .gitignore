# Never commit private/reference SQLite databases or their journal files.
__pycache__/
*.py[cod]
*.db
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal

# Local integration-test runtime artifacts.
.integration-runtime/

# Node-only test dependencies; the userscript keeps its pinned @require URLs.
node_modules/
